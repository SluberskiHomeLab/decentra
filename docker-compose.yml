version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: decentra-server
    ports:
      - "8765:8765"
    networks:
      - decentra-network
    restart: unless-stopped

networks:
  decentra-network:
    driver: bridge
